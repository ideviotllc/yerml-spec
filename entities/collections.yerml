%YERML 0.1
---

import "base.yerml"

# Collections

Set:
  items: + SetItem := r"${name}"
    name: := r".+"

Enum:
  items: + EnumItem := r"${value}:[ \t]*${name}"
      value: UInt
      name: := r".+"
